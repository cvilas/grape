{
  "version": 7,
  "cmakeMinimumRequired": { "major": 4, "minor": 1, "patch": 1 },
  "configurePresets": [
    {
      "name": "base",
      "hidden": true,
      "description": "Common configuration settings [hidden]",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "BUILD_MODULES": "all",
        "CMAKE_BUILD_TYPE": "RelWithDebInfo",
        "CMAKE_INSTALL_PREFIX": "/opt/${sourceDirName}",
        "PACKAGE_SUFFIX": "${presetName}"
      }
    },
    {
      "name": "llvm",
      "inherits": "base",
      "description": "Configure all modules with LLVM toolchain",
      "toolchainFile": "${sourceDir}/toolchains/toolchain_llvm.cmake"
    },
    {
      "name": "native",
      "inherits": "base",
      "description": "Configure all modules with native toolchain",      
      "generator": "Unix Makefiles"
    },
    {
      "name": "osx",
      "inherits": "base",
      "description": "Configure all modules with LLVM toolchain for macOS",      
      "toolchainFile": "${sourceDir}/toolchains/toolchain_osx.cmake"
    },
    {
      "name": "llvm-cross",
      "inherits": "base",
      "description": "Cross-compile for aarch64 target using LLVM toolchain",
      "toolchainFile": "${sourceDir}/toolchains/toolchain_llvm_aarch64.cmake"
    },
    {
      "name": "gcc-cross",
      "inherits": "base",
      "description": "Cross-compile for aarch64 target using GCC toolchain",
      "toolchainFile": "${sourceDir}/toolchains/toolchain_gcc_aarch64.cmake"
    }
  ]
}
